# MonarchSSD exFAT delivery, 23 September 2026

With explicit user authorization, the 1 TB Extreme 55AE SSD named MonarchSSD was erased and formatted as exFAT with a GUID partition map. The device was identified immediately before the erase by its APFS volume UUID, physical store disk6s2, physical disk model and size. MediaVault was confirmed as a separate 16 TB disk and was not formatted.

Copied the clean MediaVault publication package to /Volumes/MonarchSSD/publication_package. The copy completed successfully on 23 September at 13:43 PDT. It contains 226,837 ordinary files totaling 332,494,036,777 bytes. These comprise 226,830 photographs in 28 deployment folders, six CSV files and metadata.xml. There are no symbolic links or subdirectories inside the deployment image folders.

The destination file paths and sizes match the complete source inventory. All six CSV files and metadata.xml were also compared byte for byte and match. The XML includes the corrected classifier repository references. Finder display settings and AppleDouble files were excluded from the copy. The indexed photo set has no _02 filenames or repeated deployment timestamps.

The user cancelled full checksum verification after manually confirming the MediaVault backup. No full checksum verification was restarted. File path and size checks are not a claim of full photographic content verification. DOI and publication date placeholders in the reviewed metadata were not changed by this transfer.

Pictures, monarch-knowledge-base, other archived files and verification records remain in /Volumes/MediaVault/MonarchSSD_backup_2026-09-22. Only publication_package was copied back to the SSD. The original OneDrive shortcut and software links remain in the unrelated backup. They are not part of the delivery package. No recurring checks were recreated.

The accompanying JSON is the delivery report. A matching copy is retained on MediaVault under _verification/exfat-delivery-2026-09-23.json. The drive remains mounted for review and should be safely ejected before unplugging.
