# MediaVault release metadata update, 23 September 2026

Updated the MediaVault publication package metadata to match the repository version committed in 1f4bed3. References to the original classification JSON annotations now identify the classifier repository and its data/classifications directory. The MonarchSSD metadata already contained this update. Both external release metadata files now match data/release/metadata.xml byte for byte and parse as XML.

The metadata SHA-256 is `3d921a2f0bde98c4ca770444117bc0e0bcdf8603052e95d4c45155f7cbb92c3b`.

The user manually confirmed that the files reached MediaVault and requested cancellation of the full checksum verification. No verification process was running at the time of this request. Its last recorded failure concerned a changed Finder .DS_Store file in the parent data_release backup directory. Full checksum verification remains incomplete and was not restarted. The saved manifest describes the earlier copy and predates this metadata update.

The preceding package inventory check found all 226,830 indexed photographs in 28 deployment directories, no nested deployment directories, no symbolic links, no missing indexed files, no _02 photographs, and no repeated deployment timestamps. The six CSV files matched the reviewed versions. Two Finder .DS_Store files were removed from the publication package. This inventory check is distinct from full content verification.

The cancellation record and current metadata digest are saved outside the publication package at /Volumes/MediaVault/MonarchSSD_backup_2026-09-22/_verification/verification-cancelled-2026-09-23.json. No drive was formatted.
