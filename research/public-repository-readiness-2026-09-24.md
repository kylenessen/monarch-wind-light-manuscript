Public repository preparation, 24 September 2026

The submitted manuscript, PDF, analytical inputs, saved outputs and figures remain identical to submission commit `c105d827acfdf9cbb016e05fd111a840429dc92f`. Repository maintenance adds navigation, citation metadata, a scoped CC0 dedication for original code, recorded environments and automated Python checks. Operational notes were moved without content changes into the operations directory.

The Python checks pass all four analysis-schema tests and eighteen archive-utility tests. The utilities needed pyproj and shapely in the project test environment. These dependencies are now included in the lockfile. The R environment records the installed R 4.6.1 environment through renv. The main analysis was rerun in a temporary copy, including all 121 candidate fits. No committed scientific outputs were overwritten. The environment check verifies consistency with installed packages. A complete installation from remote R package archives on a different machine has not been tested.

The historical revision issues 34 through 43 describe the earlier review cycle. The final manuscript narrows its claims to the monitored sites and season, documents the exposure and measurement limitations, reports focused BI sensitivities, uses the audited model comparisons and removes unsupported observations and management prescriptions. Closing these revision records means the submitted revision addressed them. It does not imply that independent counts, cluster-level wind calibration or experimental validation were performed. Those limitations remain explicit. Issue 43's earlier M16 specification is superseded by the harmonized primary comparison and the separately identified no-time sensitivity.

The observational release still has the assigned publication date of September 30. DOI activation and upload are external to these repository changes. The investigator confirmed the DOI and mailed the drive on September 23. The previously noted SC6 distance discrepancy remains a release coordination item and was not silently corrected here.
